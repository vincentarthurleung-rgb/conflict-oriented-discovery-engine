# Candidate Context Blocking Dependency v1

An active dependency requires a qualified Candidate, a Context-blocked Entry,
and an existing active Observation remediation need. The dependency references
but never owns or creates that need. Unqualified Candidates cannot receive an
active L4 Context dependency.

