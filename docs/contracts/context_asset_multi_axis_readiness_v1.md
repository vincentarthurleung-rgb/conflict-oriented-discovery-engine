# Context Asset Multi-axis Readiness v1

Semantic, evidence, provenance, replayability, coverage, downstream, and future
reuse readiness are independent axes governed by a versioned threshold
contract. `usable_with_limitations` can describe a structured legacy artifact
and is not a Human Gold label.

