# Experimental observation machine reuse readiness v4 candidate

Readiness v4 is candidate-only and assigns every Observation exactly one
status. It separates local Source gaps, external Source gaps, source not
reported, core annotation pending, unresolved linkage, method limitation, and
context limitation.

Optional method enrichment is non-blocking. v4 does not replace v3 and has no
Formal or conflict authority.

