# Experimental Core Remediation Requirement v3

Core linkage and Method gaps are classified as offline resolved, annotation required, source not reported, source reingestion required, optional enrichment, Provider candidate, or unresolved. The historical v1/v2 records remain immutable.

Provider required defaults to zero. Candidate status never authorizes automatic execution, Provider calls, network access, or budget use.
