# Selective Re-extraction Requirement v2

Requirements are reconsidered in this order: authoritative raw replay, parsed
migration, anchor reconstruction, validator replay, normalization replay, and
derived-only migration. Provider re-extraction remains required unless all
needed fields are resolved by an authoritative offline mode. Every execution,
network, budget, and historical-mutation authorization is false.

