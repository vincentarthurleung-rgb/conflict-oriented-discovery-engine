# Experimental Core Remediation Requirement v2

Remediation is evaluated after all deterministic offline recovery. Core-blocking gaps and enrichment gaps are separate. Missing Method is enrichment by default. Requirements are deduplicated by source block and minimal scope.

`automatic_execution_authorized`, `provider_call_authorized`, `network_call_authorized`, and `budget_authorization_present` are always false.
