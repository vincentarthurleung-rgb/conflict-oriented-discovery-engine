# Historical Parser Replay v1

Replay reads immutable raw bytes and writes a new forensic sidecar. It records
the raw hash, historical parser/contract identity, parse output, canonical hash,
warnings, errors, and validation result. A current compatibility parser cannot
claim historical authority unless a separately documented contract proves
canonical equivalence.

