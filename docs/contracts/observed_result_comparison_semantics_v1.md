# Observed Result Comparison Semantics v1

This contract separates `comparison_required` from Result direction. Intervention/control, group/group, condition/baseline, timepoint/baseline, and dose/reference comparisons require a reference. Explicit descriptive and association policies may be exempt. Unresolved semantics remain fail-closed and are never silently treated as descriptive.
